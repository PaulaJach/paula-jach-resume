import styled from "styled-components";

const CompetenceItemContainer = styled.li`
  margin: 0.2rem 0.7rem;
  font-size: 0.9rem;
  div {
    margin: 0.2rem;
  }
`;

export default CompetenceItemContainer;
